<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cyq.product.mapper.UserMapper">

    <select id="getRolesByUserId" parameterType="integer" resultType="com.cyq.product.domain.Role">
        select r.id as id, r.role_name as roleName, r.info as info from t_user_role ur left join t_role r on ur.role_id = r.id and ur.user_id = #{userId}
    </select>

</mapper>